import './index.css'

const AppointmentItem = props => {
  const {Details} = props
  console.log(Details)
  const {id, title, date, isLiked} = Details
  return (
    <div>
      <hi>hi</hi>
    </div>
  )
}
export default AppointmentItem
